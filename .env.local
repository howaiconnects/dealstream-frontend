# DealStream - Bright Data & Supabase example env (DO NOT commit secrets to public repos)
# Copy this file to .env.local and fill with real values for local development.
# All BRIGHTDATA_* and SUPABASE_* values must remain server-side (do not expose to client).

# Bright Data - API token used for Deep Lookup, Scraper Library, IDE endpoints
BRIGHTDATA_API_TOKEN=bd_xxx_replace_with_real_token

# Bright Data - Unlocker / Proxy credentials (used for native proxy auth)
BRIGHTDATA_PROXY_HOST=brd.superproxy.io
BRIGHTDATA_PROXY_PORT=22225
BRIGHTDATA_PROXY_USER=brd_usr-xxxxx
BRIGHTDATA_PROXY_PASS=super-secret-proxy-password

# Bright Data - MCP (optional; required if you plan to forward to Web MCP)
BRIGHTDATA_MCP_URL=https://your-mcp-host.example.com
BRIGHTDATA_MCP_TOKEN=bd_mcp_xxx_replace_with_real_token

# Supabase (optional) - used for server-side logging of Bright Data interactions
SUPABASE_URL=https://isiszzxmjeqbnywhavwx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImlzaXN6enhtamVxYm55d2hhdnd4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc1NDc3MTUwNSwiZXhwIjoyMDcwMzQ3NTA1fQ.c5ITkIWjUjwwXL_G2KC5NTjd-rlYYda7KrAB4Lbgnc0
# Service Role key (server-only). Keep this secret.

# Next.js / App settings (optional)
# During local dev you can set NEXT_PUBLIC_... for non-sensitive client values.
NEXT_PUBLIC_APP_ENV=development

# Guardrails: soft caps for the console UI (optional tuning)
# These are used by the console UI (if implemented) to limit calls per tool during a test.
BD_SOFT_CAP_UNLOCKER=10
BD_SOFT_CAP_SCRAPER=10
BD_SOFT_CAP_DEEP_LOOKUP=10

# Notes:
# - After copying to .env.local, restart Next.js dev server to pick up changes.
# - Never commit .env.local with real secrets. Use environment secrets in production (Vercel/GCP/AWS).